# Public Supabase client configuration.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only optional/reserved secret for operational checks or scripts.
# Do not expose this as NEXT_PUBLIC_*.
SUPABASE_SERVICE_ROLE_KEY=

# After connecting a domain, configure Supabase Auth Site URL and Redirect URLs
# in the Supabase dashboard. The app does not require APP_URL/SITE_URL today.
